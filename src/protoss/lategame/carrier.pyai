use_lategame_vs(Terran, Protoss)

if owned(stargate):
    build(1, Fleet Beacon, 80)
    build(4, stargate, 80)
    if owned(Arbiter Tribunal):
        attack_train(2, Arbiter)
    attack_train(12, Carrier)
    attack_simple()