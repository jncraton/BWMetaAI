use_lategame_vs(Zerg, Terran, Protoss)

if owned(hydralisk den):
    if owned(defiler mound):
        attack_train(3, defiler)
    attack_train(24, Ling)
    attack_train(8, Lurker)
    attack_simple()