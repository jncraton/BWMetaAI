use_attack_vs(Zerg)

if notowns(Armory): # Building an armory is a flag that we've switched to mech
    if owned(science facility):
        if owned(control tower):
            attack_train(1, Terran Science Vessel)
    attack_multiple(3, 12 Marine, 4 Medic)
    attack_simple()