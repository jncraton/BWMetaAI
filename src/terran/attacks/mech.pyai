use_attack_vs(Terran, Protoss, Zerg)

if owned(Machine Shop):
    train(6, Siege Tank)

    if enemyownsair():
        if owned(Armory):
            attack_train(8, Goliath)

    attack_multiple(3, 6 Siege Tank, 8 Vulture)

    attack_simple()
