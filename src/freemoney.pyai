# This is included in the middle of every town manager

wait_until(8)
if random(0):
    give_money()

if random(0):
    multirun_loop:
        # provide a chance of topping up resources to 2000/2000 for free
        wait_until(4)
        wait_build(1, assimilator)
        if random(25): # 1 in 10
            give_money()
        wait(450) # 10 seconds
